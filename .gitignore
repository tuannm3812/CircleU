# macOS
.DS_Store

# Xcode user-specific state
xcuserdata/
*.xcuserstate

# Swift Package Manager / Xcode generated files
.build/
DerivedData/

# Local agent workspace artifacts
.superpowers/

# App archives and packaged output
*.xcarchive
*.ipa

# Per-developer signing override (each dev copies Signing.xcconfig.example)
Config/Signing.xcconfig

# Per-developer Firebase config (each dev registers their own iOS app and
# downloads their own GoogleService-Info.plist — see the .example file).
Circleu/GoogleService-Info.plist
